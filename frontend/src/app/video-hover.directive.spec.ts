import { VideoHoverDirective } from './video-hover.directive';

describe('VideoHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new VideoHoverDirective();
    expect(directive).toBeTruthy();
  });
});
